<?php 
	/*9) Faça um algoritmo que calcule e mostre a área de um quadrado. Sabe-se que: A = lado2;*/

	/*$lado = 5;
	$areaQuad = 0;

	$areaQuad = pow($lado, 2);

	echo "A área do quadrado é igual a ".$areaQuad."cm²"."<br>";



	echo "A área do quadrado é igual a ".pow($lado,2)."cm²";*/

	
function sum(int $a, int $b) {
    return $a + $b;
}

var_dump(sum(1, 2));

// These will be coerced to integers: note the output below!
//var_dump(sum(1.5, 2.5));


 ?>